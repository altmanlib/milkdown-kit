---
'@altmanlib/milkdown-kit-react': minor
---

Add `defaultValue` to the React `MdEditor` for uncontrolled use. `value` no longer defaults to `''`: when it is omitted the editor starts from `defaultValue` and is not synced with later props.
