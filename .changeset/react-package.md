---
'@altmanlib/milkdown-kit-react': minor
'@altmanlib/milkdown-kit': patch
'@altmanlib/milkdown-kit-vue': patch
---

Add `@altmanlib/milkdown-kit-react`, a React 19 component mirroring the Vue package API with `value` / `onChange` and a `ref` that exposes `EditorHandle`
